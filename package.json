{
  "name": "produto-backend",
  "version": "1.0.0",
  "description": "API que recebe o link de um produto e devolve nome, preço, imagem, descrição, cor, etc.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "puppeteer": "^23.0.0"
  }
}
